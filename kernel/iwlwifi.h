#ifndef IWLWIFI_H
#define IWLWIFI_H

#include <stdint.h>

#define IWLWIFI_MAX_SCAN_RESULTS 16

typedef struct {
    uint8_t bssid[6];
    char ssid[33];
    uint8_t ssid_length;
    uint8_t channel;
    int8_t rssi_dbm;
    uint16_t capability;
    uint16_t beacon_interval;
    uint8_t dtim_period;
    int rsn;
    uint8_t rsn_ie[66];
    uint8_t rsn_ie_length;
    int wpa2_psk_ccmp;
} iwlwifi_scan_result_t;

typedef struct {
    int present;
    int mmio_mapped;
    int mmio_bar_64bit;
    int mmio_above_4g;
    int firmware_valid;
    uint16_t device_id;
    uint8_t bus;
    uint8_t slot;
    uint8_t function;
    uint32_t mmio_phys;
    uint32_t firmware_size;
    uint32_t firmware_version;
    uint32_t firmware_build;
    uint32_t tlv_count;
    uint32_t runtime_sections;
    uint32_t runtime_bytes;
    uint32_t largest_runtime_section;
    int runtime_catalog_ready;
    uint32_t init_sections;
    uint32_t init_bytes;
    uint32_t largest_init_section;
    uint32_t init_separators;
    int init_catalog_ready;
    uint32_t phy_config;
    uint8_t valid_tx_ant;
    uint8_t valid_rx_ant;
    uint32_t calib_flow_trigger[2];
    uint32_t calib_event_trigger[2];
    uint32_t paging_mem_size;
    uint32_t api_bits[4];
    uint32_t capability_bits[4];
    uint32_t command_versions;
    uint8_t scan_config_version;
    uint8_t scan_request_version;
    uint8_t add_station_version;
    uint8_t phy_context_version;
    uint32_t max_scan_channels;
    int dma_staging_ready;
    uint32_t dma_staging_phys;
    uint32_t hw_revision;
    uint32_t csr_gp_control;
    uint8_t mac_address[6];
    int mac_address_valid;
    uint32_t cpu_separators;
    int rx_queue_ready;
    uint32_t rx_descriptor_phys;
    uint32_t rx_used_descriptor_phys;
    uint32_t rx_status_phys;
    uint32_t rx_buffer_count;
    uint32_t rx_buffer_size;
    int nic_access_ready;
    int hardware_rfkill;
    int rx_hw_programmed;
    uint32_t rx_write_index;
    uint32_t rx_write_actual;
    uint32_t rx_read_index;
    int firmware_upload_started;
    int firmware_upload_complete;
    int firmware_alive;
    uint16_t firmware_alive_status;
    uint16_t firmware_alive_flags;
    uint32_t firmware_notifications;
    int command_queue_allocated;
    int command_queue_dma_programmed;
    int command_queue_active;
    uint32_t command_tfd_phys;
    uint32_t command_buffer_phys;
    uint32_t keep_warm_phys;
    uint32_t command_byte_count_phys;
    uint32_t scd_base_addr;
    uint32_t command_write_index;
    uint32_t command_read_index;
    int command_echo_sent;
    int command_echo_complete;
    uint32_t command_error;
    uint32_t firmware_chunks_loaded;
    uint32_t firmware_upload_error;
    uint32_t firmware_error_section;
    uint32_t firmware_error_offset;
    uint32_t active_firmware_image;
    int nvm_ready;
    uint32_t nvm_section_mask;
    uint32_t nvm_bytes;
    uint32_t nvm_error;
    uint32_t nvm_valid_channels;
    uint8_t nvm_initial_channel;
    int init_complete;
    int phy_db_ready;
    uint32_t phy_db_sections;
    uint32_t phy_db_bytes;
    uint32_t calibration_error;
    int transport_restart_started;
    int transport_restart_complete;
    uint32_t transport_restart_error;
    int runtime_configured;
    uint32_t runtime_config_error;
    int dqa_enabled;
    int auxiliary_queue_ready;
    int auxiliary_station_ready;
    uint8_t auxiliary_station_id;
    uint8_t phy_contexts_ready;
    int scan_configured;
    uint8_t scan_config_channels;
    uint32_t scan_error;
    int scan_started;
    int scan_active;
    int scan_complete;
    uint8_t scan_complete_status;
    uint8_t scan_result_count;
    uint32_t scan_frames;
    iwlwifi_scan_result_t scan_results[IWLWIFI_MAX_SCAN_RESULTS];
    int association_context_ready;
    uint8_t association_network;
    uint32_t association_error;
    int authentication_response;
    uint16_t authentication_status;
    int association_response;
    uint16_t association_status;
    uint16_t association_id;
    int management_tx_ready;
    uint32_t management_tx_count;
    uint32_t management_tx_error;
    int link_association_started;
    int link_associated;
    int link_security_ready;
    int ap_station_ready;
    uint32_t wifi_tx_packets;
    uint32_t wifi_rx_packets;
    uint8_t wpa2_stage;
    uint32_t wpa2_error;
    int wpa2_mic_valid;
    int wpa2_pairwise_key_ready;
    int wpa2_group_key_ready;
    uint8_t wpa2_group_key_id;
    uint16_t disconnect_reason;
    uint32_t phy_db_sections_sent;
    int paging_ready;
    uint32_t paging_blocks;
    uint32_t paging_last_pages;
    uint32_t paging_error;
    char firmware_name[48];
    char firmware_description[65];
} iwlwifi_state_t;

void iwlwifi_probe(void);
void iwlwifi_set_boot_firmware(const uint8_t *data, uint32_t size);
const iwlwifi_state_t *iwlwifi_get_state(void);
int iwlwifi_start_scan(void);
int iwlwifi_prepare_association(uint8_t network_index);
int iwlwifi_associate_open(uint8_t network_index);
int iwlwifi_send_ethernet(const uint8_t *frame, uint16_t length);
int iwlwifi_connect_wpa2(uint8_t network_index, const char *passphrase);
void iwlwifi_poll(void);

#endif
